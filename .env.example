# This Vite frontend ships no private API keys or database credentials.
# Vite only exposes variables that start with MAYDARWE_PUBLIC_ (see vite.config.js).
# Never put secrets in this file or in any MAYDARWE_PUBLIC_* / VITE_* variable.
