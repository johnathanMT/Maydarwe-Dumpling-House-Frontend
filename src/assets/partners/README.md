# Delivery-partner logos

Save the official logos here as:

- `grab.svg`
- `foodpanda.svg`

That's all. The site picks them up automatically (`src/constants/partnerLogos.js`),
and the next build drops `upload.wikimedia.org` from the Content Security Policy.
